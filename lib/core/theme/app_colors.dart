import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  ////////////////////////////////////////////////////////////
  /// BACKGROUNDS
  ////////////////////////////////////////////////////////////

  /// Main atmospheric background
  static const background = Color(0xFF050913);

  /// Primary surface
  static const surface = Color(0xFF0C1422);

  /// Elevated floating surfaces
  static const elevatedSurface = Color(0xFF121B2B);

  /// Premium telemetry cards
  static const card = Color(0xFF101827);

  /// Input backgrounds
  static const inputFill = Color(0xFF131C2B);

  ////////////////////////////////////////////////////////////
  /// ACCENTS
  ////////////////////////////////////////////////////////////

  /// Soft premium cyan
  static const cyan = Color(0xFF74C7EC);

  /// Controlled atmospheric blue
  static const cyanBright = Color(0xFF5FB7DD);

  /// Deep slate blue
  static const blue = Color(0xFF3B82A6);

  ////////////////////////////////////////////////////////////
  /// TEXT
  ////////////////////////////////////////////////////////////

  /// Softer than pure white
  static const textPrimary = Color(0xFFF4F7FB);

  /// Elegant secondary hierarchy
  static const textSecondary = Color(0xB8DDE7F5);

  /// Atmospheric muted text
  static const textMuted = Color(0x669FB3C8);

  ////////////////////////////////////////////////////////////
  /// BORDERS
  ////////////////////////////////////////////////////////////

  /// Extremely subtle borders
  static const border = Color(0x10FFFFFF);

  ////////////////////////////////////////////////////////////
  /// STATUS
  ////////////////////////////////////////////////////////////

  /// Calm success tone
  static const success = Color(0xFF57C785);

  /// Muted premium warning
  static const warning = Color(0xFFE6C15A);

  /// Softer destructive tone
  static const danger = Color(0xFFE58A9B);

  ////////////////////////////////////////////////////////////
  /// ATMOSPHERIC GRADIENTS
  ////////////////////////////////////////////////////////////

  static const morningTop = Color(0xFF12233D);

  static const morningBottom = Color(0xFF29486B);

  static const afternoonTop = Color(0xFF0F1E34);

  static const afternoonBottom = Color(0xFF234061);

  static const eveningTop = Color(0xFF0A1220);

  static const eveningBottom = Color(0xFF1A2B42);

  static const nightTop = Color(0xFF03060D);

  static const nightBottom = Color(0xFF0C1626);
}
